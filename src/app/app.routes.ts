import { Routes } from '@angular/router';
import { PrincipalPagesComponent } from './pages/principal/principal-pages.component';

export const routes: Routes = [
    {
     path: 'principal',
     component: PrincipalPagesComponent   
    }
];
